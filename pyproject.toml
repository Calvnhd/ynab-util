[project]
name = "ynab-util"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
clean-csv = "clean_csv:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
